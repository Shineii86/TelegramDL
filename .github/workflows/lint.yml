name: CI/CD

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black isort
          
      - name: Run flake8 (errors only)
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          
      - name: Check formatting with black
        run: |
          black --check .
          
      - name: Check import order with isort
        run: |
          isort --check-only .
          
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install bandit
          
      - name: Run bandit security linter
        run: |
          bandit -r . -ll
          
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    needs: [lint, security]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Verify Python syntax
        run: |
          python -m py_compile bot.py
          python -m py_compile config.py
          python -m py_compile app.py
          python -m py_compile gen_session.py
          
      - name: Verify plugin syntax
        run: |
          python -m py_compile plugins/start.py
          python -m py_compile plugins/generate.py
          python -m py_compile plugins/backup.py
          python -m py_compile plugins/broadcast.py
          python -m py_compile plugins/payment.py
          python -m py_compile plugins/logger.py
          python -m py_compile plugins/ytdl.py
          python -m py_compile plugins/custom_bot.py
          python -m py_compile plugins/settings.py
          
      - name: Verify utils syntax
        run: |
          python -m py_compile utils/ui.py
          python -m py_compile utils/progress.py
          python -m py_compile utils/session.py
          python -m py_compile utils/keepalive.py
          python -m py_compile utils/checkpoint.py
          python -m py_compile utils/media.py
          python -m py_compile utils/filters.py
          python -m py_compile utils/archive.py
          python -m py_compile utils/splitter.py
          python -m py_compile utils/ytdl.py
          python -m py_compile utils/audio_metadata.py
          python -m py_compile utils/rich_message.py
          
      - name: Verify database syntax
        run: |
          python -m py_compile database/db.py
          
      - name: Build Docker image
        run: |
          docker build -t telegramdl-test .
